[
  {
    "name": "notebook",
    "request": {"code": "print('Hello, Shelby!')\n2 ** 5"},
    "expected": {"stdout": "Hello, Shelby!\n", "value": 32, "delivered": null, "error": null}
  },
  {
    "name": "arrays",
    "request": {"code": "pages = make_array(100, 200, 300)\ndeliver(np.append(pages * 2, 800))\nnp.mean(pages)"},
    "expected": {"value": 200, "delivered": {"kind": "array", "values": [200, 400, 600, 800]}, "error": null}
  },
  {
    "name": "filter-and-stable-sort",
    "request": {"code": "shelf = Table().with_columns('title', ['A', 'B', 'C', 'D'], 'pages', [200, 50, 200, 300])\ndeliver(shelf.where('pages', are.above(100)).sort('pages', descending=True))"},
    "expected": {"delivered": {"kind": "table", "labels": ["title", "pages"], "rows": [["D", 300], ["A", 200], ["C", 200]], "totalRows": 3}, "error": null}
  },
  {
    "name": "seeded-inputs",
    "request": {"code": "deliver(pages)", "inputCode": "pages = np.random.randint(0, 10, 4)", "seed": 0},
    "expected": {"delivered": {"kind": "array", "values": [5, 0, 3, 3]}, "error": null}
  },
  {
    "name": "input-overrides",
    "request": {"code": "deliver(pages)", "inputCode": "pages = np.arange(4)", "inputs": {"pages": {"kind": "array", "values": [30, 40]}}},
    "expected": {"delivered": {"kind": "array", "values": [30, 40]}, "error": null}
  },
  {
    "name": "player-files",
    "request": {"code": "from cart import arrange\ndeliver(arrange(np.array([3, 1, 2])))", "files": {"cart.py": "def arrange(values):\n    return np.sort(values)\n"}},
    "expected": {"delivered": {"kind": "array", "values": [1, 2, 3]}, "error": null}
  },
  {
    "name": "loop-and-deletion",
    "request": {"code": "total = 0\nfor i in range(4):\n    total += i\ndel i\ndeliver(total)"},
    "expected": {"delivered": 6, "error": null}
  },
  {
    "name": "numpy-nonfinite",
    "request": {"code": "deliver(np.array([1, np.nan, np.inf]))"},
    "expected": {"delivered": {"kind": "array", "values": [1, null, null]}, "error": null}
  },
  {
    "name": "bundled-csv",
    "request": {"code": "deliver(Table.read_table('shelf.csv').select('Pages').take([2, 0]))"},
    "expected": {"delivered": {"kind": "table", "labels": ["Pages"], "rows": [[96], [144]], "totalRows": 2}, "error": null}
  },
  {
    "name": "construction-inspection-rows-show",
    "request": {"code": "t = Table().with_column('messy', [' 2', '2 ', 'A', ''])\nt = t.with_columns('x', [2, 2, 1, 0], 'flag', [True, False, True, False])\nassert t.labels == ('messy', 'x', 'flag')\nassert t.num_rows == 4 and t.num_columns == 3\nassert t.row(0).item('x') == 2\nassert [r.item('x') for r in t.rows] == [2, 2, 1, 0]\nt.show(2)\ndeliver(t.select('x', 'messy').drop('messy').relabeled('x', 'value').column('value'))"},
    "expected": {"delivered": {"kind": "array", "values": [2, 2, 1, 0]}, "error": null}
  },
  {
    "name": "selection-empty-ties-nan",
    "request": {"code": "t = Table().with_columns('key', [2., np.nan, 2., 0.], 'row', [0, 1, 2, 3])\na = t.sort('key', distinct=True)\nb = t.sort('key', descending=True).take([1, 0, 1]).exclude([0])\nassert t.take([]).num_rows == 0\nassert t.where('key', 7).num_rows == 0\ndeliver(b)"},
    "expected": {"error": null}
  },
  {
    "name": "all-predicates-boundaries-and-text",
    "request": {"code": "t = Table().with_columns('n', [-1., 0., 1., np.nan])\npredicates = [are.equal_to(0), are.not_equal_to(0), are.above(0), are.above_or_equal_to(0), are.below(0), are.below_or_equal_to(0), are.between(0, 1), are.between_or_equal_to(0, 1), are.contained_in([0, 1])]\ncounts = [t.where('n', p).num_rows for p in predicates]\ntext = Table().with_column('s', [' 2', '2 ', 'A', ''])\ncounts.append(text.where('s', are.containing('2')).num_rows)\ndeliver(make_array(*counts))"},
    "expected": {"error": null}
  },
  {
    "name": "apply-row-and-multiple-columns",
    "request": {"code": "t = Table().with_columns('a', [1, 2, 3], 'b', [4, 5, 6])\nx = t.apply(lambda a, b: a + b, 'a', 'b')\ny = t.apply(lambda row: row.item('a') * row.item('b'))\ndeliver(np.append(x, y))"},
    "expected": {"delivered": {"kind": "array", "values": [5, 7, 9, 4, 10, 18]}, "error": null}
  },
  {
    "name": "group-multiple-keys-collectors-and-messy-strings",
    "request": {"code": "t = Table().with_columns('key', ['2', ' 2', '2', 'A', 'A'], 'flag', [True, False, True, False, False], 'v', [1., 2., 3., 4., np.nan])\ncounts = t.group(['key', 'flag'])\nsums = t.group(['key', 'flag'], np.sum)\nassert counts.num_rows == 3\ndeliver(sums)"},
    "expected": {"error": null},
    "events": ["group"]
  },
  {
    "name": "pivot-counts-values-zero-fill",
    "request": {"code": "t = Table().with_columns('a', ['x', 'x', 'y'], 'b', ['L', 'R', 'L'], 'v', [1, 2, 3])\ncounts = t.pivot('a', 'b')\ndeliver(t.pivot('a', 'b', values='v', collect=np.sum))"},
    "expected": {"delivered": {"kind": "table", "labels": ["b", "x", "y"], "rows": [["L", 1, 3], ["R", 2, 0]], "totalRows": 2}, "error": null},
    "events": ["pivot"]
  },
  {
    "name": "join-duplicates-unmatched-other-label",
    "request": {"code": "left = Table().with_columns('key', [2, 1, 1, 8], 'value', [20, 10, 11, 80])\nright = Table().with_columns('id', [1, 1, 2, 9], 'value', [3, 4, 5, 6])\ndeliver(left.join('key', right, 'id'))"},
    "expected": {"error": null},
    "events": ["join"]
  },
  {
    "name": "empty-advanced-operations",
    "request": {"code": "t = Table().with_columns('a', [], 'b', [])\nassert t.apply(lambda x: x, 'a').size == 0\nassert t.group('a').num_rows == 0\nassert t.pivot('a', 'b').num_rows == 0\nassert t.join('a', t) is None\ndeliver(t.sample(0))"},
    "expected": {"error": null}
  },
  {
    "name": "seeded-weighted-sampling",
    "request": {"seed": 41, "code": "t = Table().with_column('v', np.arange(5))\na = t.sample(4, with_replacement=False)\nb = t.sample(8, weights=[.1, .2, .3, .2, .2])\ndeliver(np.append(a.column('v'), b.column('v')))"},
    "expected": {"error": null},
    "events": ["sample"]
  },
  {
    "name": "sampling-oversized-error",
    "request": {"code": "Table().with_column('v', [1, 2]).sample(3, with_replacement=False)"},
    "expectedError": {"type": "ValueError"}
  },
  {
    "name": "missing-column-error",
    "request": {"code": "Table().with_column('v', [1]).group('missing')"},
    "expectedError": {"type": "ValueError"}
  },
  {
    "name": "invalid-join-key-error",
    "request": {"code": "t = Table().with_column('v', [1])\nt.join(0, t, 0)"},
    "expectedError": {"type": "KeyError"}
  },
  {
    "name": "percentile-curried-vector-nan-boundaries",
    "request": {"code": "a = percentile([0, 25, 50, 100], [1, 3, 5, 9])\nb = percentile(50)([1, np.nan, 2])\ndeliver(np.append(a, b))"},
    "expected": {"error": null},
    "events": ["percentile"]
  },
  {
    "name": "percentile-empty-error",
    "request": {"code": "percentile(50, [])"},
    "expectedError": {"type": "IndexError"}
  },
  {
    "name": "statistics-in-default-and-player-module",
    "request": {"seed": 22, "inputCode": "a = sample_proportions(50, [.2, .3, .5])", "code": "import helpers\nb = helpers.draw()\nc = sample_proportions(50, [.2, .3, .5], seed=4)\ndeliver(np.concatenate([a, b, c]))", "files": {"helpers.py": "def draw():\n    assert percentile(50, [1, 2, 3]) == 2\n    return sample_proportions(50, [.2, .3, .5])\n"}},
    "expected": {"error": null},
    "events": ["sample_proportions"]
  },
  {
    "name": "sample-proportions-zero-and-error",
    "request": {"code": "zero = sample_proportions(0, [.2, .8], seed=3)\ntry: sample_proportions(-1, [.2, .8])\nexcept ValueError: print('invalid count')\ndeliver(zero)"},
    "expected": {"delivered": {"kind": "array", "values": [null, null]}, "stdout": "invalid count\n", "error": null}
  },
  {
    "name": "minimize-powell-and-bfgs",
    "request": {"code": "a = minimize(lambda x: (x-3)**2)\nb = minimize(lambda x: (x+2)**2, smooth=True)\nassert abs(a-3) < 1e-5 and abs(b+2) < 1e-5\ndeliver(make_array(round(a, 5), round(b, 5)))"},
    "expected": {"delivered": {"kind": "array", "values": [3, -2]}, "error": null},
    "events": ["minimize"]
  },
  {
    "name": "minimize-budget-result",
    "request": {"code": "logs = []\nanswer = minimize(lambda x: (x-3)**2, options={'maxfev': 1}, log=logs.append)\nassert not logs[0].success and logs[0].status == 1\ndeliver(answer)"},
    "expected": {"delivered": 0, "error": null},
    "events": ["minimize"]
  },
  {
    "name": "charts-multiple-series-unequal-bins-and-settings",
    "request": {"code": "t = Table().with_columns('x', [1, 2, 4], 'y', [2, -1, 5], 'z', [4, 3, 1])\nt.barh('x', ['y', 'z'], overlay=True, title='Values')\nt.hist('y', 'z', bins=[-2, 0, 3, 7], overlay=False)\nt.scatter('x', ['y', 'z'], fit_line=True, xlim=[0, 5])\nt.plot('x', ['y', 'z'], overlay=False)\ndeliver(1)"},
    "expected": {"delivered": 1, "error": null},
    "events": ["barh", "hist", "scatter", "plot"]
  },
  {
    "name": "chart-bounded-snapshots",
    "request": {"code": "t = Table().with_columns('x', np.arange(200), 'y', np.arange(200) * 2)\nt.plot('x')\ndeliver(t.num_rows)"},
    "expected": {"delivered": 200, "error": null},
    "events": ["plot"]
  },
  {
    "name": "numpy-aliases-native-callbacks-and-lazy-map",
    "request": {"code": "from numpy import arange as steps\nassert steps is np.arange\niterator = map(steps, [2, 3])\na = next(iterator)\nb = next(iterator)\nc = list(filter(np.isfinite, [1, np.nan, 2]))\nd = sorted([1, 3, 2], key=np.negative)\ndeliver(np.concatenate([a, b, c, d]))"},
    "expected": {"error": null},
    "events": ["numpy"]
  },
  {
    "name": "native-callback-api-lock",
    "request": {"code": "list(map(np.arange, [2, 3]))", "allowedApi": []},
    "expectedError": {"type": "LockedAPIError"}
  },
  {
    "name": "numpy-consumers",
    "request": {"code": "a = np.arange(6).reshape(2, 3)\nb = np.apply_along_axis(np.mean, 1, a)\nc = np.vectorize(np.mean, otypes=[float])(b)\ndeliver(c)"},
    "expected": {"error": null}
  },
  {
    "name": "recursive-loop-invocations",
    "request": {"code": "def f(n):\n    for i in range(2):\n        if n: f(n - 1)\n        if i: return\nf(3)\ndeliver(1)"},
    "expected": {"error": null}
  },
  {
    "name": "generator-scopes",
    "request": {"code": "def g(n):\n    a = np.arange(n)\n    for i in range(n): yield a[i]\na, b = g(2), g(4)\nprint(next(a), next(b), next(b), next(a))\na.close()\nprint(next(b))\nb.close()\ndeliver(1)"},
    "expected": {"error": null}
  },
  {
    "name": "numpy-keyword-inputs-errors",
    "request": {"code": "a = np.arange(4)\ntry: np.random.choice(a, 8, replace=False)\nexcept ValueError: print('caught')\nout = np.zeros(1)\nnp.mean(a=a, out=out.reshape(()))\ndeliver(out)"},
    "expected": {"error": null}
  },
  {
    "name": "module-loop-attribution",
    "request": {"code": "import helper\ndeliver(np.append(helper.data(), 4))", "files": {"helper.py": "def data():\n    for i in range(1):\n        return np.arange(3)\n"}},
    "expected": {"error": null}
  },
  {
    "name": "mixed-numeric-text-and-explicit-dtypes",
    "request": {"code": "a = np.asarray([1, ' 2', False])\nt = Table().with_column('x', a)\nassert t.column('x').tolist() == ['1', ' 2', 'False']\nassert np.asarray([1,2], dtype='int64').dtype.name == 'int64'\nassert np.asarray([1,2], dtype='U11').dtype.name == 'str352'\ndeliver(a)"},
    "expected": {"error": null}
  },
  {
    "name": "locked-canonical-alias",
    "request": {"code": "from numpy import arange as f\nf(3)", "allowedApi": ["np.arange"]},
    "expected": {"error": null}
  }
]
