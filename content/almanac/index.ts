export interface AlmanacEntry {
  id: string;
  signature: string;
  explanation: string;
  example: string;
  output: string;
  pitfalls: string[];
  pitfallsAfter?: string;
  parameters?: { name: string; explanation: string }[];
  comparison?: { id: string; explanation: string; example: string; output: string };
  note?: string;
}

export const almanac: AlmanacEntry[] = [
  { id: 'expressions', signature: 'name = expression', explanation: 'Give a result a name so the next line can use it. Python computes the right side first.', example: 'drops = 4 * 3\ndrops + 2', output: '14', pitfalls: ['= assigns a name; == compares two values.'] },
  { id: 'deliver', signature: 'deliver(value)', explanation: 'Hand a value to the waiting patron for checking.', example: 'deliver(2 + 5)', output: '7', pitfalls: ['Printing a value does not deliver it.'] },
  { id: 'print', signature: 'print(*values)', explanation: 'Write a note in the output panel while investigating your script.', example: "print('Tiles:', 4)", output: 'Tiles: 4', pitfalls: ['Printed output is separate from the delivered answer.'] },
  { id: 'division', signature: 'number / divisor; number // divisor', explanation: '/ keeps fractional shares; // rounds the quotient down.', example: '7 / 2\n7 // 2', output: '3.5\n3', pitfalls: ['Floor division loses the fractional part, even if an earlier shelf divided evenly.'], pitfallsAfter: 'p0-02-shares' },
  { id: 'round', signature: 'round(number, ndigits=0)', explanation: 'Round a number to a chosen number of decimal places.', example: 'round(8.376, 2)', output: '8.38', pitfalls: ['Rounding early can lose information; only round when requested.'] },
  { id: 'abs', signature: 'abs(number)', explanation: 'Return the distance from zero.', example: 'abs(-7)', output: '7', pitfalls: ['The sign disappears.'] },
  { id: 'max', signature: 'max(values) or max(a, b, ...)', explanation: 'Find the greatest value.', example: 'max(4, 9, 2)', output: '9', pitfalls: ['Text uses alphabetical comparison, not numeric magnitude.'] },
  { id: 'min', signature: 'min(values) or min(a, b, ...)', explanation: 'Find the smallest value.', example: 'min(4, 9, 2)', output: '2', pitfalls: ['An empty collection has no minimum.'] },
  { id: 'len', signature: 'len(collection)', explanation: 'Count the items in a collection, or characters in text.', example: "len('Moss')", output: '4', pitfalls: ['The count is one past the final index.'], pitfallsAfter: 'ch1-vary-1' },
  { id: 'str', signature: 'str(value)', explanation: 'Represent a value as text.', example: "'Tray-' + str(8)", output: "'Tray-8'", pitfalls: ['Adding text and an integer raises TypeError.'], pitfallsAfter: 'p0-03-badge' },
  { id: 'int', signature: 'int(value)', explanation: 'Read whole-number text as an integer, or truncate a numeric value toward zero.', example: "int('14') + 2", output: '16', pitfalls: ["int('3.5') fails; decimal text needs float first.", 'Comparing numeric text compares characters.'], pitfallsAfter: 'p0-04-budget' },
  { id: 'float', signature: 'float(value)', explanation: 'Read a decimal quantity from text.', example: "float('2.75') + 1", output: '3.75', pitfalls: ['Binary floating-point arithmetic can leave tiny rounding differences.'] },
  { id: 'comparisons', signature: 'a < b; a <= b; a == b; a >= b; a > b', explanation: 'Ask a yes-or-no question and get True or False.', example: '5 <= 5', output: 'True', pitfalls: ['Equality is included by <= and >=, but not < and >.'] },
  { id: 'make_array', signature: 'make_array(*elements)', explanation: 'Collect values into a NumPy array, a tray whose elements share a type.', example: 'make_array(3, 6, 9)', output: 'array([3, 6, 9])', pitfalls: ['Mixing text and numbers can convert every element to text.'], pitfallsAfter: 'ch1-break-2' },
  { id: 'array-math', signature: 'array * number; array + array', explanation: 'Apply arithmetic to every tile at once and return a new array.', example: 'make_array(2, 5) * 3', output: 'array([6, 15])', pitfalls: ['Two arrays must have compatible lengths.'], pitfallsAfter: 'ch1-break-1' },
  {
    id: 'np.arange', signature: 'np.arange(start, stop, step)',
    explanation: 'Generate an array of evenly spaced numbers. Start is included; stop is not.',
    parameters: [
      { name: 'start', explanation: 'The first number. Here, 2.' },
      { name: 'stop', explanation: 'The boundary to stop before. Here, 10 is left out.' },
      { name: 'step', explanation: 'How much to add each time. Here, add 4. If omitted, it is 1.' },
    ],
    example: 'import numpy as np\nnp.arange(2, 10, 4)', output: 'array([2, 6])',
    comparison: {
      id: 'make_array', explanation: 'make_array keeps the exact values you list. np.arange generates values by following a pattern.',
      example: 'make_array(2, 10, 4)', output: 'array([2, 10, 4])',
    },
    note: 'For this request’s positive whole-number step, use last + 1 as stop to allow the last plate.',
    pitfalls: ['The stop itself is never included for integer steps.'], pitfallsAfter: 'ch1-show-2',
  },
  { id: 'item', signature: 'array.item(index)', explanation: 'Take one scalar from a zero-based position in an array.', example: 'make_array(8, 13).item(1)', output: '13', pitfalls: ['The final nonnegative index is len(array) - 1.'], pitfallsAfter: 'ch1-vary-1' },
  { id: 'sum', signature: 'sum(values)', explanation: 'Add all the values together.', example: 'sum(make_array(2, 4, 7))', output: '13', pitfalls: ['A total and a mean answer different questions.'] },
  { id: 'np.mean', signature: 'np.mean(array)', explanation: 'Divide a total by the number of elements.', example: 'np.mean(make_array(2, 4, 9))', output: '5.0', pitfalls: ['An average need not be one of the observed values.'] },
  { id: 'np.count_nonzero', signature: 'np.count_nonzero(array)', explanation: 'Count nonzero values; on a boolean tray this counts True.', example: 'np.count_nonzero(make_array(1, 5, 7) >= 5)', output: '2', pitfalls: ['Use the comparison requested, including or excluding equality deliberately.'] },
  { id: 'matching-lengths', signature: 'len(left) == len(right)', explanation: 'Check that two trays pair one tile to one tile.', example: 'len(make_array(1, 2)) == len(make_array(3, 4))', output: 'True', pitfalls: ['A fixed-length companion fails when a new shelf has another size.'], pitfallsAfter: 'ch1-break-1' },
  { id: 'mixed-types', signature: 'make_array(int(a), int(b))', explanation: 'Convert numeric text before choosing a shared numeric array type.', example: "make_array(int('8'), int('3')) + 1", output: 'array([9, 4])', pitfalls: ['Digit-shaped strings still do not support numeric array arithmetic.'], pitfallsAfter: 'ch1-break-2' },
  { id: 'Table', signature: 'Table()', explanation: 'Create an empty table, ready for named columns.', example: 'Table().with_columns("title", make_array("Cloud"), "pages", make_array(80))', output: 'title | pages\nCloud | 80', pitfalls: ['Each column must have the same number of rows.'] },
  { id: 'with_columns', signature: 'table.with_columns(label, values, ...)', explanation: 'Return a table with added or replaced columns.', example: "Table().with_columns('pages', make_array(40, 90))", output: 'pages\n40\n90', pitfalls: ['Keep the returned table; the original is unchanged.'] },
  { id: 'column', signature: 'table.column(label)', explanation: 'Take a single column as an array.', example: "Table().with_columns('n', make_array(2, 8)).column('n')", output: 'array([2, 8])', pitfalls: ['An array no longer carries table column labels.'] },
  { id: 'select', signature: 'table.select(*labels)', explanation: 'Return a new table containing the requested columns in the requested order.', example: "Table().with_columns('a', make_array(1), 'b', make_array(2)).select('b')", output: 'b\n2', pitfalls: ['Calling select alone leaves the original table intact.'], pitfallsAfter: 'ch2-show-1' },
  { id: 'drop', signature: 'table.drop(*labels)', explanation: 'Return a table without selected columns.', example: "Table().with_columns('a', make_array(1), 'b', make_array(2)).drop('a')", output: 'b\n2', pitfalls: ['Column labels are exact, including case.'] },
  { id: 'relabeled', signature: 'table.relabeled(old, new)', explanation: 'Return a table with a different column label.', example: "Table().with_columns('n', make_array(3)).relabeled('n', 'count')", output: 'count\n3', pitfalls: ['Values stay unchanged; later operations must use the new label.'] },
  { id: 'labels', signature: 'table.labels', explanation: 'Inspect the column names in order.', example: "Table().with_columns('n', make_array(3)).labels", output: "('n',)", pitfalls: ['This is a property, so it has no call parentheses.'] },
  { id: 'num_rows', signature: 'table.num_rows', explanation: 'Count the books in a table.', example: "Table().with_columns('n', make_array(3, 4)).num_rows", output: '2', pitfalls: ['A filtered result can have zero rows.'] },
  { id: 'num_columns', signature: 'table.num_columns', explanation: 'Count the fields on each catalog card.', example: "Table().with_columns('n', make_array(3)).num_columns", output: '1', pitfalls: ['This counts columns, not books.'] },
  { id: 'where', signature: 'table.where(label, predicate)', explanation: 'Copy matching rows to a new table, leaving the source untouched.', example: "Table().with_columns('n', make_array(1, 4)).where('n', are.above(2))", output: 'n\n4', pitfalls: ['An empty result is valid and retains its labels.'], pitfallsAfter: 'ch2-show-2' },
  { id: 'sort', signature: 'table.sort(label, descending=False, distinct=False)', explanation: 'Return rows ordered by a column; ascending is the default.', example: "Table().with_columns('n', make_array(4, 1)).sort('n')", output: 'n\n1\n4', pitfalls: ['Sorting text digits is different from sorting numeric values.'] },
  { id: 'take', signature: 'table.take(indices)', explanation: 'Return rows at the given zero-based positions.', example: "Table().with_columns('n', make_array(4, 9)).take(1)", output: 'n\n9', pitfalls: ['A position beyond the final row raises IndexError.'] },
  { id: 'are.above_or_equal_to', signature: 'are.above_or_equal_to(value)', explanation: 'Build a predicate that keeps the boundary and everything above it.', example: "Table().with_columns('n', make_array(2, 3)).where('n', are.above_or_equal_to(3))", output: 'n\n3', pitfalls: ['A strict comparison would lose ties at the cutoff.'], pitfallsAfter: 'ch2-show-2' },
  { id: 'are.above', signature: 'are.above(value)', explanation: 'Build a predicate for strictly greater values.', example: "Table().with_columns('n', make_array(2, 3)).where('n', are.above(2))", output: 'n\n3', pitfalls: ['The boundary itself does not pass.'] },
  { id: 'are.equal_to', signature: 'are.equal_to(value)', explanation: 'Build a predicate for exact equality.', example: "Table().with_columns('n', make_array(2, 3)).where('n', are.equal_to(2))", output: 'n\n2', pitfalls: ['Text equality is case-sensitive.'] },
  { id: 'are.not_equal_to', signature: 'are.not_equal_to(value)', explanation: 'Keep every row except the ones matching a value.', example: "Table().with_columns('n', make_array(2, 3)).where('n', are.not_equal_to(2))", output: 'n\n3', pitfalls: ['Rows with missing or differently spelled labels also survive.'] },
  { id: 'are.below', signature: 'are.below(value)', explanation: 'Build a predicate for strictly smaller values.', example: "Table().with_columns('n', make_array(2, 5)).where('n', are.below(5))", output: 'n\n2', pitfalls: ['The cutoff itself is excluded.'] },
  { id: 'are.below_or_equal_to', signature: 'are.below_or_equal_to(value)', explanation: 'Keep the boundary and everything under it.', example: "Table().with_columns('n', make_array(2, 5)).where('n', are.below_or_equal_to(2))", output: 'n\n2', pitfalls: ['Choosing the strict form silently drops ties at the boundary.'] },
  { id: 'are.between', signature: 'are.between(low, high)', explanation: 'Keep values at or above the low end and strictly below the high end.', example: "Table().with_columns('n', make_array(1, 4, 9)).where('n', are.between(2, 9))", output: 'n\n4', pitfalls: ['The upper end is open, so a value equal to high is dropped.'] },
  { id: 'are.between_or_equal_to', signature: 'are.between_or_equal_to(low, high)', explanation: 'Keep a closed range, both ends included.', example: "Table().with_columns('n', make_array(1, 4, 9)).where('n', are.between_or_equal_to(4, 9))", output: 'n\n4\n9', pitfalls: ['Swapping low and high yields an empty table rather than an error.'] },
  { id: 'are.strictly_between', signature: 'are.strictly_between(low, high)', explanation: 'Keep values strictly inside a range, excluding both ends.', example: "Table().with_columns('n', make_array(1, 4, 9)).where('n', are.strictly_between(1, 9))", output: 'n\n4', pitfalls: ['Both endpoints disappear, which can surprise a count.'] },
  { id: 'are.containing', signature: 'are.containing(substring)', explanation: 'Keep rows whose text holds a fragment anywhere inside it.', example: "Table().with_columns('t', make_array('Moss', 'Fern')).where('t', are.containing('os'))", output: 't\nMoss', pitfalls: ['Matching is case-sensitive and works only on text columns.'] },
  { id: 'are.contained_in', signature: 'are.contained_in(collection)', explanation: 'Keep rows whose value appears in a collection of allowed values.', example: "Table().with_columns('t', make_array('Moss', 'Fern')).where('t', are.contained_in(['Fern']))", output: 't\nFern', pitfalls: ['A string collection matches substrings, not whole entries.'] },
  { id: 'with_column', signature: 'table.with_column(label, values)', explanation: 'Return a table with one column added or replaced.', example: "Table().with_column('n', make_array(1, 2))", output: 'n\n1\n2', pitfalls: ['Reusing an existing label overwrites that column.'] },
  { id: 'exclude', signature: 'table.exclude(indices)', explanation: 'Return every row except the given positions.', example: "Table().with_columns('n', make_array(4, 9)).exclude(0)", output: 'n\n9', pitfalls: ['Positions refer to the current order, so sort first if order matters.'] },
  { id: 'row', signature: 'table.row(index)', explanation: 'Read one row as a tuple-like record with named fields.', example: "Table().with_columns('n', make_array(4, 9)).row(1).item(0)", output: '9', pitfalls: ['A row is not a table; table methods do not apply to it.'] },
  { id: 'rows', signature: 'table.rows', explanation: 'Step through every row in order, usually in a for loop.', example: "len(Table().with_columns('n', make_array(4, 9)).rows)", output: '2', pitfalls: ['Row-by-row loops are slower than whole-column arithmetic.'] },
  { id: 'apply', signature: 'table.apply(function, *labels)', explanation: 'Call a function once per row and collect the results as an array.', example: "Table().with_columns('n', make_array(1, 2)).apply(lambda n: n * 10, 'n')", output: 'array([10, 20])', pitfalls: ['Pass the function itself, without calling it.', 'The argument order must match the labels given.'] },
  { id: 'group', signature: 'table.group(label, collect=None)', explanation: 'Collapse rows sharing a value into one row per group, counting them or collecting another column.', example: "Table().with_columns('w', make_array('a', 'a', 'b')).group('w')", output: 'w | count\na | 2\nb | 1', pitfalls: ['Without collect the new column is named count.', 'Grouping on a numeric column treats each distinct number as its own group.'] },
  { id: 'group-two', signature: 'table.group(make_array(label, label), collect=None)', explanation: 'Group by a pair of columns to get one row per observed combination.', example: "Table().with_columns('a', make_array(1, 1), 'b', make_array('x', 'x')).group(make_array('a', 'b'))", output: 'a | b | count\n1 | x | 2', pitfalls: ['Combinations that never occur are simply absent, not zero.'] },
  { id: 'pivot', signature: 'table.pivot(columns, rows, values=None, collect=None)', explanation: 'Lay one grouping across the columns and another down the rows.', example: "Table().with_columns('c', make_array('x', 'y'), 'r', make_array('a', 'a')).pivot('c', 'r')", output: 'r | x | y\na | 1 | 1', pitfalls: ['Empty cells become zero, which is different from missing.', 'values and collect must be supplied together.'] },
  { id: 'join', signature: 'table.join(label, other, other_label=None)', explanation: 'Match rows from two tables on a shared column.', example: "Table().with_columns('k', make_array(1)).join('k', Table().with_columns('k', make_array(1), 'v', make_array('a')))", output: 'k | v\n1 | a', pitfalls: ['Unmatched rows on either side are dropped.', 'Duplicate keys multiply the rows that come out.'] },
  { id: 'sample', signature: 'table.sample(k=None, with_replacement=True)', explanation: 'Draw rows at random; the default draws as many rows as the table has, with replacement.', example: "Table().with_columns('n', make_array(1, 2)).sample(1).num_rows", output: '1', pitfalls: ['Each call gives a different sample unless the request fixes the seed.', 'Sampling without replacement cannot exceed the number of rows.'] },
  { id: 'hist', signature: 'table.hist(label, bins=None, unit=None)', explanation: 'Draw a histogram whose bar areas, not heights, carry the proportions.', example: "Table().with_columns('n', make_array(1, 2, 2, 3)).hist('n', bins=np.arange(1, 5, 1))", output: 'chart: histogram of n', pitfalls: ['Unequal bin widths change heights; read area instead.', 'Values on a bin edge fall into the bin on the right.'] },
  { id: 'scatter', signature: 'table.scatter(x_label, select=None, fit_line=False)', explanation: 'Plot one point per row to look for association between two columns.', example: "Table().with_columns('x', make_array(1, 2), 'y', make_array(2, 4)).scatter('x', 'y')", output: 'chart: scatter of y against x', pitfalls: ['Association is not causation.', 'A fitted line is meaningless for a clearly curved cloud.'] },
  { id: 'plot', signature: 'table.plot(x_label, select=None)', explanation: 'Draw a line chart for values that follow an ordered column such as a date.', example: "Table().with_columns('t', make_array(1, 2), 'v', make_array(3, 5)).plot('t', 'v')", output: 'chart: line of v against t', pitfalls: ['Sort by the x column first, or the line doubles back on itself.'] },
  { id: 'barh', signature: 'table.barh(categories, select=None)', explanation: 'Draw horizontal bars for a categorical column.', example: "Table().with_columns('c', make_array('a', 'b'), 'n', make_array(2, 5)).barh('c', 'n')", output: 'chart: bars of n by c', pitfalls: ['Bars compare categories, while a histogram describes one numeric column.'] },
  { id: 'show', signature: 'table.show(max_rows=0)', explanation: 'Print a table in the output panel without delivering it.', example: "Table().with_columns('n', make_array(1)).show()", output: 'n\n1', pitfalls: ['Showing is not delivering; the patron still waits.'] },
  { id: 'percentile', signature: 'percentile(p, array)', explanation: 'Return the smallest value at or above which p percent of the sorted data lies.', example: 'percentile(50, make_array(1, 3, 7))', output: '3', pitfalls: ['This definition always returns an element of the data, unlike an interpolated median.'] },
  { id: 'sample_proportions', signature: 'sample_proportions(sample_size, probabilities)', explanation: 'Simulate one random sample from a categorical distribution and return the observed proportions.', example: 'sum(sample_proportions(100, make_array(0.5, 0.5)))', output: '1.0', pitfalls: ['The probabilities must sum to one.', 'Proportions come back, not counts; multiply by the sample size.'] },
  { id: 'minimize', signature: 'minimize(function, start=None)', explanation: 'Search numerically for the argument that makes a function smallest.', example: 'round(minimize(lambda x: (x - 3) ** 2), 3)', output: '3.0', pitfalls: ['A poor starting point can settle in a local dip.', 'The result is approximate, so compare with a tolerance.'] },
  { id: 'np.std', signature: 'np.std(array)', explanation: 'Measure spread as the root mean square distance from the average.', example: 'np.std(make_array(2, 4))', output: '1.0', pitfalls: ['This is the population standard deviation, dividing by n.'] },
  { id: 'np.median', signature: 'np.median(array)', explanation: 'Take the middle value of the sorted data, averaging the two middles for an even count.', example: 'np.median(make_array(1, 3, 7, 9))', output: '5.0', pitfalls: ['The median can differ from percentile(50, ...) for even counts.'] },
  { id: 'np.max', signature: 'np.max(array)', explanation: 'Take the largest element of an array.', example: 'np.max(make_array(2, 9))', output: '9', pitfalls: ['An empty array has no maximum and raises an error.'] },
  { id: 'np.min', signature: 'np.min(array)', explanation: 'Take the smallest element of an array.', example: 'np.min(make_array(2, 9))', output: '2', pitfalls: ['Text arrays compare alphabetically.'] },
  { id: 'np.sum', signature: 'np.sum(array)', explanation: 'Add every element of an array.', example: 'np.sum(make_array(2, 9))', output: '11', pitfalls: ['Summing a boolean array counts the True values.'] },
  { id: 'np.sqrt', signature: 'np.sqrt(value_or_array)', explanation: 'Take square roots, element by element for arrays.', example: 'np.sqrt(make_array(4, 9))', output: 'array([2., 3.])', pitfalls: ['Negative inputs produce nan rather than an exception.'] },
  { id: 'np.append', signature: 'np.append(array, value)', explanation: 'Return a longer array with a value or array added at the end.', example: 'np.append(make_array(1, 2), 3)', output: 'array([1, 2, 3])', pitfalls: ['The original array is unchanged; keep the returned one.', 'Appending inside a loop copies each time, so prefer whole-array work when possible.'] },
  { id: 'np.diff', signature: 'np.diff(array)', explanation: 'Return the step from each element to the next, one shorter than the input.', example: 'np.diff(make_array(2, 5, 11))', output: 'array([3, 6])', pitfalls: ['The result loses one element, so it no longer pairs with the original column.'] },
  { id: 'np.cumsum', signature: 'np.cumsum(array)', explanation: 'Return the running total at each position.', example: 'np.cumsum(make_array(1, 2, 3))', output: 'array([1, 3, 6])', pitfalls: ['The final entry equals the plain sum.'] },
  { id: 'np.random.choice', signature: 'np.random.choice(array, size=None)', explanation: 'Draw random elements from an array, with replacement by default.', example: 'np.random.choice(make_array(1, 2), 3).size', output: '3', pitfalls: ['Repeats are expected with replacement.', 'The request fixes the seed, so a run is reproducible but not identical across shelves.'] },
  { id: 'np.round', signature: 'np.round(array, decimals=0)', explanation: 'Round every element of an array.', example: 'np.round(make_array(1.234, 5.678), 1)', output: 'array([1.2, 5.7])', pitfalls: ['Half-way values round to the nearest even number.'] },
  { id: 'np.average', signature: 'np.average(array, weights=None)', explanation: 'Average values, optionally weighting each one.', example: 'np.average(make_array(1, 3), weights=make_array(3, 1))', output: '1.5', pitfalls: ['Weights must be non-negative and the same length as the values.'] },
  { id: 'boolean-arrays', signature: 'array > value', explanation: 'Compare a whole array at once to get a tray of True and False.', example: 'make_array(1, 5) > 2', output: 'array([False,  True])', pitfalls: ['Use np.count_nonzero or np.mean on the result rather than a loop.'] },
  { id: 'standard-units', signature: '(array - np.mean(array)) / np.std(array)', explanation: 'Restate values as distances from the average in standard deviations, so two different columns become comparable.', example: 'np.mean((make_array(2, 4) - np.mean(make_array(2, 4))) / np.std(make_array(2, 4)))', output: '0.0', pitfalls: ['Standard units have mean zero and standard deviation one by construction.', 'A constant column has zero spread and cannot be standardised.'] },
  { id: 'correlation', signature: 'np.mean(su_x * su_y)', explanation: 'Average the product of two columns in standard units to measure straight-line association between -1 and 1.', example: 'round(np.mean(make_array(-1.0, 1.0) * make_array(-1.0, 1.0)), 2)', output: '1.0', pitfalls: ['A correlation near zero rules out a line, not a curve.', 'Correlation is unchanged by switching the two columns.'] },
  { id: 'regression-line', signature: 'slope = r * np.std(y) / np.std(x)', explanation: 'Predict one column from another with the line through the averages in standard units.', example: 'round(0.8 * 4 / 2, 2)', output: '1.6', pitfalls: ['The intercept must use the same slope and both averages.', 'Predicting far outside the observed range is guesswork.'] },
  { id: 'residuals', signature: 'residual = actual - predicted', explanation: 'Look at what the line missed, one row at a time.', example: 'np.mean(make_array(2, -2))', output: '0.0', pitfalls: ['Residuals for a fitted line average to zero, so judge their pattern rather than their mean.', 'Curved residuals mean a line was the wrong shape.'] },
  { id: 'for-loop', signature: 'for item in collection:', explanation: 'Repeat a block once per item, usually to collect simulation results.', example: 'total = 0\nfor n in make_array(1, 2, 3):\n    total = total + n\ntotal', output: '6', pitfalls: ['Values created inside the loop vanish unless collected outside it.', 'np.arange(n) counts from zero to n - 1.'] },
  { id: 'conditionals', signature: 'if condition:\n    ...\nelse:\n    ...', explanation: 'Choose between two paths based on a yes-or-no question.', example: "n = 5\n'big' if n > 3 else 'small'", output: "'big'", pitfalls: ['A comparison on a whole array is not a single True or False.'] },
  { id: 'def', signature: 'def name(parameters):\n    return value', explanation: 'Name a small procedure so a request, or table.apply, can reuse it.', example: 'def doubled(n):\n    return n * 2\n\ndoubled(7)', output: '14', pitfalls: ['A function with no return hands back None.', 'Pass the function to apply without calling it.'] },
  { id: 'simulation', signature: 'results = np.append(results, statistic)', explanation: 'Repeat a random experiment many times and gather each outcome for a histogram.', example: 'len(np.append(make_array(), 1.0))', output: '1', pitfalls: ['Too few repetitions make a jagged, unreliable picture.', 'Each repetition must redraw the randomness, not reuse one sample.'] },
  { id: 'bootstrap', signature: 'table.sample()', explanation: 'Resample the sample with replacement to see how much a statistic could have varied.', example: "Table().with_columns('n', make_array(1, 2, 3)).sample().num_rows", output: '3', pitfalls: ['Sampling without replacement rebuilds the original sample and shows no variation.', 'A bootstrap interval describes the parameter, not future single observations.'] },
  { id: 'confidence-interval', signature: 'make_array(percentile(2.5, stats), percentile(97.5, stats))', explanation: 'Take the middle 95 percent of the bootstrap statistics as an interval estimate.', example: 'percentile(2.5, make_array(1, 2, 3, 4))', output: '1', pitfalls: ['The interval covers the parameter for most samples, not with 95 percent probability for this one.', 'A wider interval buys confidence with precision.'] },
  { id: 'hypothesis-test', signature: 'np.count_nonzero(simulated >= observed) / len(simulated)', explanation: 'Compare an observed statistic with statistics simulated under the null model to get a p-value.', example: 'np.count_nonzero(make_array(1, 5, 9) >= 5) / 3', output: '0.6666666666666666', pitfalls: ['The tail direction must match how the statistic was defined.', 'A large p-value fails to reject the model; it does not prove it.'] },
  { id: 'tvd', signature: 'sum(np.abs(a - b)) / 2', explanation: 'Measure the distance between two distributions by halving the total absolute difference.', example: 'sum(np.abs(make_array(0.5, 0.5) - make_array(0.25, 0.75))) / 2', output: '0.25', pitfalls: ['Forgetting the halving doubles every distance.', 'Both distributions must be proportions over the same categories.'] },
  { id: 'range', signature: 'range(start, stop, step=1)', explanation: 'Count through whole numbers for a loop, including start and excluding stop.', example: 'total = 0\nfor i in range(3):\n    total = total + i\ntotal', output: '3', pitfalls: ['range(3) gives 0, 1, 2 — never 3.', 'A range is not an array; wrap it in make_array to do arithmetic on it.'] },
  { id: 'read_table', signature: 'Table.read_table(path)', explanation: 'Load a comma-separated file from the shelf into a table.', example: "Table.read_table('members.csv').num_columns", output: '2', pitfalls: ['Columns of digits stored with stray text load as strings.', 'The file must be one of the request\u2019s bundled files.'] },
  { id: 'np.sort', signature: 'np.sort(array)', explanation: 'Return a new array with the values in ascending order.', example: 'np.sort(make_array(4, 1, 9))', output: 'array([1, 4, 9])', pitfalls: ['The original array keeps its order; keep the returned one.', 'Sorting one array alone breaks its pairing with another column.'] },
  { id: 'np.abs', signature: 'np.abs(array)', explanation: 'Take the distance from zero for every element.', example: 'np.abs(make_array(-2, 3))', output: 'array([2, 3])', pitfalls: ['Signs vanish, so opposite deviations stop cancelling.'] },
  { id: 'np.absolute', signature: 'np.absolute(array)', explanation: 'The long spelling of np.abs: absolute value, element by element.', example: 'np.absolute(make_array(-5))', output: 'array([5])', pitfalls: ['It behaves identically to np.abs; pick one spelling and stay with it.'] },
  { id: 'np.percentile', signature: 'np.percentile(array, p)', explanation: 'NumPy\u2019s interpolating percentile, which can return a value between two observations.', example: 'np.percentile(make_array(1, 3), 50)', output: '2.0', pitfalls: ['This differs from percentile(p, array), which always returns an observed value.'] },
  { id: 'standard-deviation', signature: 'np.std(array)', explanation: 'A single number for how far values typically sit from their average.', example: 'np.std(make_array(2, 2, 6, 6))', output: '2.0', pitfalls: ['Spread is unchanged when every value shifts by the same amount.', 'One extreme value can inflate it sharply.'] },
  { id: 'chebyshev', signature: 'at least 1 - 1 / z ** 2 within z standard deviations', explanation: 'A guarantee that holds for any distribution: at least 75 percent of values lie within two standard deviations of the average, and 89 percent within three.', example: 'round(1 - 1 / 3 ** 2, 2)', output: '0.89', pitfalls: ['It is a floor, not an estimate; most data crowd far closer.', 'It says nothing at all for z of one or less.'] },
  { id: 'square-root-law', signature: 'np.std(sample_means) = sd / np.sqrt(sample_size)', explanation: 'Averages wobble less than single observations, and the wobble shrinks with the square root of the sample size.', example: 'round(8 / np.sqrt(16), 2)', output: '2.0', pitfalls: ['Quartering the wobble needs sixteen times the sample, not four.', 'The law describes the average, not the underlying spread of the data.'] },
  { id: 'wobble-of-an-average', signature: 'sd_of_sample_mean = sd / np.sqrt(n)', explanation: 'The spread of a sample average across repeated samples, which is what an interval for a mean has to cover.', example: 'round(10 / np.sqrt(100), 2)', output: '1.0', pitfalls: ['A larger sample does not change the spread of the data itself.'] },
  { id: 'outlier-flags', signature: "table.where('su', are.above(3))", explanation: 'Flag rows whose standard units sit implausibly far from the average so they can be inspected rather than silently kept.', example: 'np.count_nonzero(make_array(0.2, 4.1) > 3)', output: '1', pitfalls: ['Flagging is not deleting; a real extreme value may belong in the answer.', 'Standard units must come from the same column being judged.'] },
  { id: 'test-statistic', signature: 'statistic = f(sample)', explanation: 'One number chosen in advance that gets larger when the data disagree with the null model.', example: 'abs(np.mean(make_array(4, 6)) - 4)', output: '1.0', pitfalls: ['Choosing the statistic after seeing the data invalidates the test.', 'A two-sided question needs a statistic that ignores direction.'] },
  { id: 'null-model', signature: 'shuffled = table.sample(with_replacement=False)', explanation: 'The chance model that says the interesting pattern is only luck; simulating it shows what luck alone produces.', example: "Table().with_columns('n', make_array(1, 2)).sample(with_replacement=False).num_rows", output: '2', pitfalls: ['The simulation must keep everything fixed except the thing assumed random.'] },
  { id: 'p-value', signature: 'np.count_nonzero(simulated >= observed) / len(simulated)', explanation: 'The chance the null model produces a statistic at least as extreme as the one observed.', example: 'np.count_nonzero(make_array(1, 2, 9) >= 2) / 3', output: '0.6666666666666666', pitfalls: ['A small p-value is evidence against the model, not proof of a cause.', 'The observed value itself counts as at least as extreme.'] },
  { id: 'tail-direction', signature: 'simulated >= observed or simulated <= observed', explanation: 'Which tail counts depends on how the statistic was defined; a distance statistic always uses the upper tail.', example: 'np.count_nonzero(make_array(1, 5) <= 1) / 2', output: '0.5', pitfalls: ['Counting the wrong tail turns strong evidence into none.'] },
  { id: 'permutation-test', signature: 'table.with_column(label, shuffled_labels)', explanation: 'Shuffle the group labels to simulate a world where the grouping made no difference.', example: "Table().with_columns('g', make_array('a', 'b')).sample(with_replacement=False).num_rows", output: '2', pitfalls: ['Shuffling with replacement changes the group sizes and breaks the test.'] },
  { id: 'total-variation-distance', signature: 'sum(np.abs(a - b)) / 2', explanation: 'Distance between two categorical distributions, from 0 when identical to 1 when they share nothing.', example: 'sum(np.abs(make_array(0.6, 0.4) - make_array(0.4, 0.6))) / 2', output: '0.2', pitfalls: ['Both inputs must be proportions over the same categories in the same order.'] },
  { id: 'confounding', signature: 'compare within each group', explanation: 'A third factor that differs between the groups can produce or reverse a difference that looks causal.', example: "Table().with_columns('room', make_array('a', 'b')).num_rows", output: '2', pitfalls: ['An observational comparison cannot rule out an unmeasured confounder.', 'Aggregated totals can reverse every within-group comparison.'] },
  { id: 'resample', signature: 'table.sample()', explanation: 'One draw of the same size from the sample itself, with replacement, standing in for a fresh trip to the population.', example: "Table().with_columns('n', make_array(1, 2, 3)).sample().num_rows", output: '3', pitfalls: ['A resample smaller than the original understates the true wobble.', 'Sampling without replacement simply rebuilds the sample.'] },
  { id: 'bootstrap-statistic', signature: 'stats = np.append(stats, f(resample))', explanation: 'Recompute the statistic of interest on every resample and collect the results.', example: 'len(np.append(make_array(), 2.5))', output: '1', pitfalls: ['Recompute the same statistic the question asked about, not a convenient one.', 'Too few resamples make the interval jump between runs.'] },
  { id: 'interval-decision', signature: 'low <= claim <= high', explanation: 'A 95 percent interval doubles as a test: a claimed value outside the interval is rejected at the 5 percent level.', example: '1 <= 4 <= 3', output: 'False', pitfalls: ['A value on the boundary is inside the interval, so it is not rejected.', 'Failing to reject is not the same as confirming the claim.'] },
  { id: 'two-group-bootstrap', signature: 'resample each group separately', explanation: 'Bootstrap a difference between two groups by resampling inside each group, keeping the group sizes.', example: 'round(6.0 - 4.5, 2)', output: '1.5', pitfalls: ['Pooling both groups before resampling destroys the difference being measured.', 'An interval containing zero leaves the direction undecided.'] },
  { id: 'least-squares', signature: 'minimize(lambda a, b: mse(a, b))', explanation: 'Choose the slope and intercept that make the mean squared error smallest.', example: 'round(minimize(lambda x: (x - 2) ** 2), 2)', output: '2.0', pitfalls: ['Squared error punishes one far-off row much harder than several near ones.', 'The numeric search is approximate; compare fits with a tolerance.'] },
  { id: 'prediction', signature: 'predicted = slope * x + intercept', explanation: 'Read a value off the fitted line for a given x.', example: 'round(1.5 * 4 + 2, 2)', output: '8.0', pitfalls: ['A prediction is an average outcome, not a promise about one book.'] },
  { id: 'extrapolation-guard', signature: 'np.min(x) <= x_new <= np.max(x)', explanation: 'Only trust a fitted line inside the range of x actually observed.', example: 'np.min(make_array(2, 9)) <= 12 <= np.max(make_array(2, 9))', output: 'False', pitfalls: ['The line happily returns a number well outside the data; the number is the guess, not the evidence.'] },
  { id: 'influence', signature: 'compare the fit with and without the row', explanation: 'A single far-out row can swing a fitted line; refitting without it shows how much.', example: 'round(abs(1.6 - 0.4), 2)', output: '1.2', pitfalls: ['A high-leverage row is not automatically an error to delete.', 'Report that a row was removed and why.'] },
  { id: 'distance', signature: '((x1 - x2) ** 2 + (y1 - y2) ** 2) ** 0.5', explanation: 'Straight-line distance between two records described by numeric attributes.', example: 'round(((3 - 0) ** 2 + (4 - 0) ** 2) ** 0.5, 1)', output: '5.0', pitfalls: ['Distance grows with every attribute added, so compare like with like.'] },
  { id: 'nearest-neighbour', signature: 'table.sort("distance").row(0)', explanation: 'Classify a new record with the label of the single closest training record.', example: 'np.argmin(make_array(2.5, 0.8, 4.0))', output: '1', pitfalls: ['One neighbour follows noise closely.', 'The closest record may still be far away.'] },
  { id: 'k-nearest-neighbours', signature: 'table.sort("distance").take(np.arange(k))', explanation: 'Take the majority label among the k closest training records.', example: 'np.count_nonzero(make_array(1, 1, 0) == 1)', output: '2', pitfalls: ['An even k can tie; choose an odd k or a stated tie rule.', 'A large k smooths away real local structure.'] },
  { id: 'tie-breaks', signature: 'odd k, or a declared rule', explanation: 'When neighbours split evenly the classifier must fall back on a rule chosen in advance.', example: 'np.count_nonzero(make_array(1, 1, 0, 0) == 1)', output: '2', pitfalls: ['Silently taking the first row makes the answer depend on table order.'] },
  { id: 'feature-scaling', signature: '(column - np.mean(column)) / np.std(column)', explanation: 'Put every attribute in standard units so a large-numbered column cannot dominate the distance.', example: 'round(np.std((make_array(2, 4) - 3) / 1.0), 2)', output: '1.0', pitfalls: ['Unscaled page counts swamp shelf numbers.', 'Scale the new record with the training column\u2019s average and spread.'] },
  { id: 'train-test-split', signature: 'shuffled.take(np.arange(k)) and the rest', explanation: 'Hold part of the data back so accuracy is measured on records the classifier never saw.', example: 'len(np.arange(4))', output: '4', pitfalls: ['Splitting an unshuffled table can put one whole class in the test set.', 'A record in both halves leaks the answer.'] },
  { id: 'accuracy', signature: 'np.count_nonzero(predicted == actual) / len(actual)', explanation: 'The share of held-out records the classifier labels correctly.', example: 'np.count_nonzero(make_array(1, 0, 1) == make_array(1, 1, 1)) / 3', output: '0.6666666666666666', pitfalls: ['Accuracy on training rows is optimistic.', 'With lopsided classes, always guessing the common one already scores well.'] },
  { id: 'chance-tree', signature: 'branch chance = prior * likelihood', explanation: 'Draw each outcome as a path and multiply the chances along it.', example: 'round(0.02 * 0.9, 3)', output: '0.018', pitfalls: ['Chances along a path multiply; chances across paths add.', 'Every branch out of one point must total one.'] },
  { id: 'total-chance', signature: 'sum of every path that reaches the outcome', explanation: 'Add the chances of all the different ways an outcome can happen.', example: 'round(0.02 * 0.9 + 0.98 * 0.05, 4)', output: '0.067', pitfalls: ['Missing one path understates the total.', 'The paths must not overlap.'] },
  { id: 'base-rate', signature: 'prior = rare / population', explanation: 'How common the condition is before any evidence, which usually dominates the answer.', example: 'round(1 / 500, 4)', output: '0.002', pitfalls: ['A rare condition plus an imperfect test makes most positives false.', 'Ignoring the base rate is the classic mistake.'] },
  { id: 'bayes-rule', signature: 'posterior = path / total_chance', explanation: 'Divide the chance of the path you care about by the chance of everything that matches the evidence.', example: 'round(0.018 / 0.067, 3)', output: '0.269', pitfalls: ['The denominator is the total chance of the evidence, not of the condition.'] },
  { id: 'posterior', signature: 'updated chance given the evidence', explanation: 'The revised chance after the evidence is in; it replaces the prior for the next question.', example: 'round(0.018 / 0.067, 2)', output: '0.27', pitfalls: ['A posterior can stay small even after a strong positive result.'] },
  { id: 'sequential-update', signature: 'prior = posterior; repeat', explanation: 'Fold in a second piece of evidence by making the first posterior the new prior.', example: 'round(0.27 * 0.9 / (0.27 * 0.9 + 0.73 * 0.05), 2)', output: '0.87', pitfalls: ['Reusing the original base rate for the second test throws away the first result.', 'Updating twice with the same evidence double-counts it.'] },
  { id: 'chute-board', signature: 'left chance and right chance at each pin', explanation: 'The branching-chute board in the Atlas Deck: a physical chance tree a slip really falls through.', example: 'round(0.5 * 0.5, 2)', output: '0.25', pitfalls: ['The two chutes rarely take equal shares; read the board before assuming a half.'] },
  { id: 'capstone-bookends', signature: 'make_array(percentile(2.5, stats), percentile(97.5, stats))', explanation: 'The capstone bookends: a bootstrap interval for a fitted slope, set on the shelf as two ends of one estimate.', example: 'percentile(2.5, make_array(1.1, 1.4, 1.9, 2.2))', output: '1.1', pitfalls: ['Refit the line inside every resample; resampling the residuals alone answers a different question.', 'An interval for a slope says nothing about one book.'] },
  { id: 'classification', signature: 'distances = ((x - row_x) ** 2 + (y - row_y) ** 2) ** 0.5', explanation: 'Classify a new record by the labels of its nearest neighbours in the training table.', example: 'round(((3 - 0) ** 2 + (4 - 0) ** 2) ** 0.5, 1)', output: '5.0', pitfalls: ['Attributes on different scales must be standardised first.', 'Accuracy must be measured on rows the classifier never saw.'] },
  { id: 'strip', signature: 'text.strip()', explanation: 'Return text with spaces and other whitespace removed from both ends.', example: "' Fern '.strip()", output: "'Fern'", pitfalls: ['Spaces inside the text stay in place.', 'Keep the returned string; the original is unchanged.'], pitfallsAfter: 'ch2-break-2' },
  { id: 'lower', signature: 'text.lower()', explanation: 'Return text with letters converted to lowercase, so differently capitalized labels can match.', example: "'FeRn'.lower()", output: "'fern'", pitfalls: ['Lowercase text can still have leading or trailing spaces; combine with strip when needed.'], pitfallsAfter: 'ch2-break-2' },
];

export function visibleAlmanac(unlocked: readonly string[], completed: readonly string[]): AlmanacEntry[] {
  return almanac.filter((entry) => unlocked.includes(entry.id)).map((entry) => ({
    ...entry,
    pitfalls: !entry.pitfallsAfter || completed.includes(entry.pitfallsAfter) ? entry.pitfalls : [],
  }));
}
